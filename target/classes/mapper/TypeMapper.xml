<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.weixi.blog.mapper.TypeMapper">

    <!-- 结果映射 -->
    <resultMap id="TypeMap" type="com.weixi.blog.entity.Type">
        <id column="id" property="id"/>
        <result column="user_id" property="userId"/>
        <result column="name" property="name"/>
        <result column="description" property="description"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
    </resultMap>

    <!-- 根据用户ID查询所有分类 -->
    <select id="selectByUserId" resultMap="TypeMap">
        SELECT 
            id,
            user_id,
            name,
            description,
            create_time,
            update_time
        FROM t_type
        WHERE user_id = #{userId}
        ORDER BY create_time DESC
    </select>

    <!-- 查询所有分类（已废弃，保留兼容性） -->
    <select id="selectAll" resultMap="TypeMap">
        SELECT 
            id,
            user_id,
            name,
            description,
            create_time,
            update_time
        FROM t_type
        ORDER BY create_time DESC
    </select>

    <!-- 插入分类 -->
    <insert id="insert" parameterType="com.weixi.blog.entity.Type" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO t_type (user_id, name, description, create_time, update_time)
        VALUES (#{userId}, #{name}, #{description}, NOW(), NOW())
    </insert>

    <!-- 更新分类 -->
    <update id="updateById" parameterType="com.weixi.blog.entity.Type">
        UPDATE t_type
        SET name = #{name},
            description = #{description},
            update_time = NOW()
        WHERE id = #{id} AND user_id = #{userId}
    </update>

    <!-- 删除分类 -->
    <delete id="deleteById">
        DELETE FROM t_type
        WHERE id = #{id} AND user_id = #{userId}
    </delete>

    <!-- 根据ID和用户ID查询分类 -->
    <select id="selectByIdAndUserId" resultMap="TypeMap">
        SELECT 
            id,
            user_id,
            name,
            description,
            create_time,
            update_time
        FROM t_type
        WHERE id = #{id} AND user_id = #{userId}
    </select>

</mapper>
