<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.weixi.blog.mapper.TagMapper">

    <!-- 结果映射 -->
    <resultMap id="TagMap" type="com.weixi.blog.entity.Tag">
        <id column="id" property="id"/>
        <result column="user_id" property="userId"/>
        <result column="name" property="name"/>
        <result column="color" property="color"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
    </resultMap>

    <!-- 根据用户ID查询所有标签 -->
    <select id="selectByUserId" resultMap="TagMap">
        SELECT 
            id,
            user_id,
            name,
            color,
            create_time,
            update_time
        FROM t_tag
        WHERE user_id = #{userId}
        ORDER BY create_time DESC
    </select>

    <!-- 查询所有标签（已废弃，保留兼容性） -->
    <select id="selectAll" resultMap="TagMap">
        SELECT 
            id,
            user_id,
            name,
            color,
            create_time,
            update_time
        FROM t_tag
        ORDER BY create_time DESC
    </select>

    <!-- 插入标签 -->
    <insert id="insert" parameterType="com.weixi.blog.entity.Tag" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO t_tag (user_id, name, color, create_time, update_time)
        VALUES (#{userId}, #{name}, #{color}, NOW(), NOW())
    </insert>

    <!-- 更新标签 -->
    <update id="updateById" parameterType="com.weixi.blog.entity.Tag">
        UPDATE t_tag
        SET name = #{name},
            color = #{color},
            update_time = NOW()
        WHERE id = #{id} AND user_id = #{userId}
    </update>

    <!-- 删除标签 -->
    <delete id="deleteById">
        DELETE FROM t_tag
        WHERE id = #{id} AND user_id = #{userId}
    </delete>

    <!-- 根据ID和用户ID查询标签 -->
    <select id="selectByIdAndUserId" resultMap="TagMap">
        SELECT 
            id,
            user_id,
            name,
            color,
            create_time,
            update_time
        FROM t_tag
        WHERE id = #{id} AND user_id = #{userId}
    </select>

</mapper>
